import { atom } from 'recoil';

export const headerHide = atom({
  key: 'headerHide',
  default: false,
});

export const headerMyPage = atom({
  key: 'x',
  default: false,
});

export const headerNotification = atom({
  key: 'headerNotification',
  default: false,
});

export const headerLang = atom({
  key: 'headerLang',
  default: false,
});

export const headerAccount = atom({
  key: 'headerAccount',
  default: false,
});
