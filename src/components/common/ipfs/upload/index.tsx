import styled from '@emotion/styled';

export type IpfsFileUploadProps = {
  // TODO
};

export const IpfsFileUpload = (props: IpfsFileUploadProps) => {


  return (
    <Container />
  );
};

const Container = styled.div([]);
