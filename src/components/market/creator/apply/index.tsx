import styled from '@emotion/styled';

export type MarketCreatorApplyProps = {
  // TODO
};

export const MarketCreatorApply = (props: MarketCreatorApplyProps) => {
  return (
    <Container>Market Creator Apply</Container>
  );
};

const Container = styled.div([]);
